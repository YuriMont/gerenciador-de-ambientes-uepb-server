# URL base da API (backend em apps/server).
# O prefixo VITE_ é obrigatório: sem ele o Vite não expõe a variável ao browser.
# Também é lida pelo orval.config.ts (via dotenv) para buscar o OpenAPI em /v3/api-docs.
VITE_API_URL=http://localhost:8080
